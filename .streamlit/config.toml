[theme]
base = "dark"
# Indigo — keeps Streamlit accents consistent (avoids orange send/chat chrome)
primaryColor = "#818cf8"
backgroundColor = "#0b0f1a"
secondaryBackgroundColor = "rgba(255, 255, 255, 0.04)"
textColor = "rgba(255, 255, 255, 0.92)"

[client]
toolbarMode = "minimal"
